<?xml version="1.0" encoding="utf-8"?>
<manifest package="test.nice.testproject"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:name=".DemoApplication"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.EspressoTestTheme">
        <activity
            android:name=".activities.MainActivity">
        </activity>
        <activity
            android:name=".activities.SwitchActivity">
        </activity>
        <activity
            android:name=".activities.NavigationDrawerActivity">
        </activity>
        <activity
            android:name=".activities.PositionActivity">
        </activity>
        <activity
            android:name=".activities.ViewPagerActivity">
        </activity>
        <activity
            android:name=".activities.HierarchyActivity">
        </activity>
        <activity
            android:name=".activities.CustomIntentActivity"/>
        <activity android:name=".dagger.DaggerActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>

    </application>

</manifest>
